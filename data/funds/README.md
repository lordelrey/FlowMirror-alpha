# Fund inputs

`nav_demo_2025q4.json` is deterministic synthetic data used only by the offline examples. It must not be interpreted as historical or current fund performance.

Users who run their own scenarios should supply legally obtained market data through a local path and keep that file outside version control.
